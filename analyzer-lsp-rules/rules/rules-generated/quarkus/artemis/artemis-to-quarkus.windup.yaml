- - message: Replace the `org.apache.activemq:artemis-jms-client` dependency with
      the Quarkus dependency `io.quarkus:quarkus-artemis-jms`. Further information
      in the link below.
    ruleID: ../../windup/windup-rulesets/rules/rules-generated/quarkus/artemis/artemis-to-quarkus.windup.xml-0
    when:
      java.dependency:
        lowerbound: 0.0.0
        name: org.apache.activemq.artemis-jms-client
  - message: Replace the `org.apache.activemq:artemis-jms-client` dependency with
      the Quarkus dependency `io.quarkus:quarkus-artemis-jms`. Further information
      in the link below.
    ruleID: ../../windup/windup-rulesets/rules/rules-generated/quarkus/artemis/artemis-to-quarkus.windup.xml-1
    when:
      java.dependency:
        lowerbound: 0.0.0
        name: org.apache.activemq.artemis-jms-client
