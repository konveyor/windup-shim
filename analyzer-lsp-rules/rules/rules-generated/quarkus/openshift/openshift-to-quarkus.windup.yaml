- - message: Replace the `io.fabric8:openshift-client` dependency with the Quarkus
      dependency `io.quarkus:quarkus-openshift-client`. Further information in the
      link below.
    ruleID: ../../windup/windup-rulesets/rules/rules-generated/quarkus/openshift/openshift-to-quarkus.windup.xml-0
    when:
      java.dependency:
        name: io.fabric8.openshift-client
  - message: Replace the `io.fabric8:openshift-client` dependency with the Quarkus
      dependency `io.quarkus:quarkus-openshift-client`. Further information in the
      link below.
    ruleID: ../../windup/windup-rulesets/rules/rules-generated/quarkus/openshift/openshift-to-quarkus.windup.xml-1
    when:
      java.dependency:
        name: io.fabric8.openshift-client
