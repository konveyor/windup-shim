- - message: The application uses a JDBC connection string.. Checkout the different
      types of databases that are fully managed on Azure.
    ruleID: ../../windup/windup-rulesets/rules/rules-reviewed/azure/springboot/spring-boot-to-azure-database.windup.xml-0
    when:
      builtin.filecontent:
        pattern: 'jdbc:'
  - message: The application uses a MongoDB connection string.. Checkout the different
      types of databases that are fully managed on Azure.
    ruleID: ../../windup/windup-rulesets/rules/rules-reviewed/azure/springboot/spring-boot-to-azure-database.windup.xml-1
    when:
      builtin.filecontent:
        pattern: 'mongodb:'
