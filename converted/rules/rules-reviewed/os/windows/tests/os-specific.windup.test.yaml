testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/os/windows/tests/data
rulePath:
- ../os-specific.windup.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: This file system path is Microsoft Windows platform dependent.*
            size: 4
      perform:
        fail:
        - message: Windows platform dependent hint was not found!
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: This Dynamic-Link Library is Microsoft Windows platform dependent.*
            size: 1
      perform:
        fail:
        - message: Dynamic-Link Library hint was not found!
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/os/windows/tests/os-specific.windup.test.xml
