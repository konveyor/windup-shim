testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/eapxp/eapxp2/tests/data/data-eapxp_bootable_jar_maven_plugin_configuration_changes
rulePath:
- ../eapxp_bootable_jar_maven_plugin_configuration_changes.mta.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP XP 3.0 \(or later\),
                and want to ensure its Maven building, running or testing works as
                expected, rename the configuration element `extraServerContent` to
                `extraServerContentDirs`.
            size: 1
      perform:
        fail:
        - message: Bootable JAR Maven Plugin configuration with element `extraServerContent`
            not found in pom.xml
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/eapxp/eapxp2/tests/eapxp_bootable_jar_maven_plugin_configuration_changes.mta.test.xml
