testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/eap7/eap72/tests/data/data-maven-javax-to-jakarta
rulePath:
- ../maven-javax-to-jakarta.rhamt.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3 \(or later\),
                and want to ensure its Maven building, running or testing works as
                expected, use instead the Jakarta EE dependency with groupId `com.sun.mail`,
                and artifactId `jakarta.mail`.
            size: 1
      perform:
        fail:
        - message: No reference to `com.sun.mail:javax.mail` found in pom.xml
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3, or later,
                and want to ensure its Maven building, running or testing works as
                expected, use instead the Jakarta EE dependency with groupId `com.sun.activation`
            size: 1
      perform:
        fail:
        - message: maven-javax-to-jakarta-00002-test - failed
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3, or later,
                and want to ensure its Maven building, running or testing works as
                expected, use instead the Jakarta EE dependency with artifactId `jakarta.activation`
            size: 1
      perform:
        fail:
        - message: maven-javax-to-jakarta-00003-test - failed
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3, or later,
                and want to ensure its Maven building, running or testing works as
                expected, use instead the Jakarta EE dependency - groupId `jakarta.
            size: 7
      perform:
        fail:
        - message: maven-javax-to-jakarta-00004-test - failed
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3, or later,
                and want to ensure its Maven building, running or testing works as
                expected, use instead the Jakarta EE dependency with groupId `jakarta.
            size: 4
      perform:
        fail:
        - message: maven-javax-to-jakarta-00005-test - failed
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3, or later,
                and want to ensure its Maven building, running or testing works as
                expected, use instead the Jakarta EE dependency with artifactId `jakarta.enterprise.cdi-api`
            size: 1
      perform:
        fail:
        - message: maven-javax-to-jakarta-00006-test - failed
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3, or later,
                and want to ensure its Maven building, running or testing works as
                expected, use instead the Jakarta EE dependency with artifactId `jakarta.validation-api`
            size: 1
      perform:
        fail:
        - message: maven-javax-to-jakarta-00007-test - failed
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3, or later,
                and want to ensure its Maven building, running or testing works as
                expected, use instead the Jakarta EE dependency with artifactId `jakarta.inject-api`
            size: 1
      perform:
        fail:
        - message: maven-javax-to-jakarta-00008-test - failed
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3 \(or later\),
                and want to ensure its Maven building, running or testing works as
                expected, use instead the Jakarta EE dependency with groupId `org.jboss.spec.javax.xml.soap`,
                and artifactId `jboss-saaj-api_1.4_spec`.
            size: 1
      perform:
        fail:
        - message: No reference to `org.jboss.spec.javax.xml.soap:jboss-saaj-api_1.3_spec`
            found in pom.xml
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3 \(or later\),
                and want to ensure its Maven building, running or testing works as
                expected, use instead the Jakarta EE dependency with groupId `org.jboss.spec.javax.transaction`,
                and artifactId `jboss-transaction-api_1.3_spec`.
            size: 1
      perform:
        fail:
        - message: No reference to `org.jboss.spec.javax.transaction:jboss-transaction-api_1.2_spec`
            found in pom.xml
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3 \(or later\),
                and want to ensure its Maven building, running or testing works as
                expected, use instead the JBoss EAP Jakarta EE BOM with groupId `org.jboss.bom`,
                and artifactId `jboss-eap-jakartaee8`.
            size: 1
      perform:
        fail:
        - message: No reference to `org.jboss.bom:jboss-eap-javaee8` found in pom.xml
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3 \(or later\),
                and want to ensure its Maven building, running or testing works as
                expected, use instead the JBoss EAP Jakarta EE BOM with groupId `org.jboss.bom`,
                and artifactId `jboss-eap-jakartaee8-with-spring4`.
            size: 1
      perform:
        fail:
        - message: No reference to `org.jboss.bom:jboss-eap-javaee8-with-spring` found
            in pom.xml
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3 \(or later\),
                and want to ensure its Maven building, running or testing works as
                expected, use instead the JBoss EAP Jakarta EE BOM with groupId `org.jboss.bom`,
                and artifactId `jboss-eap-jakartaee8-with-tools`.
            size: 1
      perform:
        fail:
        - message: No reference to `org.jboss.bom:jboss-eap-javaee8-with-tools` found
            in pom.xml
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3 \(or later\),
                and want to ensure its Maven building, running or testing works as
                expected, use instead the JBoss Jakarta EE BOM with groupId `org.jboss.spec`,
                and artifactId `jboss-jakartaee-8.0`.
            size: 1
      perform:
        fail:
        - message: No reference to `org.jboss.spec:jboss-javaee-8.0` found in pom.xml
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3 \(or later\),
                and want to ensure its Maven building, running or testing works as
                expected, use instead the JBoss Jakarta EE BOM with groupId `org.jboss.spec`,
                and artifactId `jboss-jakartaee-web-8.0`.
            size: 1
      perform:
        fail:
        - message: No reference to `org.jboss.spec:jboss-javaee-web-8.0` found in
            pom.xml
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: If you migrate your application to JBoss EAP 7.3 \(or later\),
                and want to ensure its Maven building, running or testing works as
                expected, use instead the JBoss Jakarta EE BOM with groupId `org.jboss.spec`,
                and artifactId `jboss-jakartaee-all-8.0`.
            size: 1
      perform:
        fail:
        - message: No reference to `org.jboss.spec:jboss-javaee-all-8.0` found in
            pom.xml
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/eap7/eap72/tests/maven-javax-to-jakarta.rhamt.test.xml
