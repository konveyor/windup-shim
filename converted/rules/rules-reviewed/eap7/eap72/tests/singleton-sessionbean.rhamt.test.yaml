testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/eap7/eap72/tests/data/singleton-sessionbean
rulePath:
- ../singleton-sessionbean.rhamt.xml
- ../singleton-sessionbean.rhamt.groovy
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: When a singleton EJB bean class implements `javax.ejb.SessionBean`
                interface, this interface should be removed from the implements clause.*
            size: 2
      perform:
        fail:
        - message: No hint found for `javax.ejb.SessionBean` interface removed
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/eap7/eap72/tests/singleton-sessionbean.rhamt.test.xml
