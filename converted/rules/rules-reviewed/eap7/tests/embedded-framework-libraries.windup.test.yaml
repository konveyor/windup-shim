testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/eap7/tests/data/embedded-framework-libraries
rulePath:
- ../embedded-framework-libraries.windup.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - classification-exists:
          - classification: Embedded library - Drools
      perform:
        fail:
        - message: Drools classification not found!
    - when:
        not:
        - classification-exists:
          - classification: Hibernate embedded library
      perform:
        fail:
        - message: Hibernate embedded library classification not found!
    - when:
        not:
        - classification-exists:
          - classification: Embedded library - JDBC
      perform:
        fail:
        - message: Embedded library - JDBC classification not found!
    - when:
        not:
        - classification-exists:
          - classification: JSF embedded library
      perform:
        fail:
        - message: JSF embedded library Classification not found!
    - when:
        not:
        - classification-exists:
          - classification: Seam 2 embedded library
      perform:
        fail:
        - message: Seam 2 embedded library classification not found!
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/eap7/tests/embedded-framework-libraries.windup.test.xml
