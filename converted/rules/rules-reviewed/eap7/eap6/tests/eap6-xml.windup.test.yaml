testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/eap7/eap6/tests/data
ruleset:
- rules:
    rule:
    - when:
        not:
        - classification-exists:
          - classification: JMS proprietary resource definitions
      perform:
        fail:
        - message: Classification for all the *-jms.xml files not found!
    - when:
        not:
        - iterable-filter:
          - to-file-model:
            - classification-exists:
              - classification: JBoss deployment structure dependencies \(jboss-deployment-structure.xml\)
            size: 3
      perform:
        fail:
        - message: Classification jboss deployment structure not found!
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/eap7/eap6/tests/eap6-xml.windup.test.xml
