testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/eap8/eap7/tests/data/empty-beans-xml
rulePath:
- ../empty-beans-xml.windup.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Add a `beans` element with a `bean-discovery-mode='all'` attribute
                to an empty beans.xml file
            size: 1
      perform:
        fail:
        - message: '[empty-beans-xml-00001] The hint was not found!'
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/eap8/eap7/tests/empty-beans-xml.windup.test.xml
