testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/eap8/eap7/tests/data/dependencies/pom.xml
rulePath:
- ../javax-to-jakarta-dependencies.windup.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Update the group dependency by replacing the `javax.
            size: 26
      perform:
        fail:
        - message: '[javax-to-jakarta-dependencies-00001] The hint was not found!'
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Update artifact dependency by replacing the `javax.
            size: 26
      perform:
        fail:
        - message: '[javax-to-jakarta-dependencies-00002] The hint was not found!'
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Replace the `jaxb-api`
            size: 1
      perform:
        fail:
        - message: '[javax-to-jakarta-dependencies-00003] The hint was not found!'
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Replace dependency groupId `javax.activation`
            size: 1
      perform:
        fail:
        - message: '[javax-to-jakarta-dependencies-00004] The hint was not found!'
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Replace the `javax.faces`
            size: 1
      perform:
        fail:
        - message: '[javax-to-jakarta-dependencies-00005] The hint was not found!'
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Update group dependency by replacing the `javax` groupId with
                `jakarta.platform`
            size: 2
      perform:
        fail:
        - message: '[javax-to-jakarta-dependencies-00006] The hint was not found!'
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Update artifact dependency by replacing the `javaee-api` artifactId
                with `jakarta.jakartaee-api
            size: 1
      perform:
        fail:
        - message: '[javax-to-jakarta-dependencies-00007] The hint was not found!'
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Update artifact dependency by replacing the `javaee-web-api`
                artifactId
            size: 1
      perform:
        fail:
        - message: '[javax-to-jakarta-dependencies-00008] The hint was not found!'
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/eap8/eap7/tests/javax-to-jakarta-dependencies.windup.test.xml
