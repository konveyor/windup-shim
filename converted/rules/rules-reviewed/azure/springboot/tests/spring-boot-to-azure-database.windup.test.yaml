testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/azure/springboot/tests/data/spring-boot-to-azure-database
rulePath:
- ../spring-boot-to-azure-database.windup.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: The application uses a JDBC connection string
            size: 3
      perform:
        fail:
        - message: JDBC hint was not found!
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: The application uses a MongoDB connection string
            size: 1
      perform:
        fail:
        - message: Mongo hint was not found!
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/azure/springboot/tests/spring-boot-to-azure-database.windup.test.xml
