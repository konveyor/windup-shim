testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/openshift/tests/data
rulePath:
- ../java-rmi.windup.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - hint-exists:
          - message: Java RMI is a tightly coupled service. Tight coupling is
      perform:
        fail:
        - message: Java RMI API hint was not found!
    - when:
        not:
        - classification-exists:
          - classification: Java Remote Method Invocation \(RMI\) API
      perform:
        fail:
        - message: Java RMI classification was not found!
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/openshift/tests/java-rmi.windup.test.xml
