testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/openshift/tests/data
rulePath:
- ../mail.windup.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: In a cloud environment, mail systems should be considered backing
                services*
            size: 17
      perform:
        fail:
        - message: JavaMail hint was not found
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/openshift/tests/mail.windup.test.xml
