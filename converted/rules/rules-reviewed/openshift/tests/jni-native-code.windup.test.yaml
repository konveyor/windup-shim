testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/openshift/tests/data
rulePath:
- ../jni-native-code.windup.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Java native libraries might not run in a cloud or container
                environment.
            size: 12
      perform:
        fail:
        - message: Java JNI/JNA usage was not found
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/openshift/tests/jni-native-code.windup.test.xml
