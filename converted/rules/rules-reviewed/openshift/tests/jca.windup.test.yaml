testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/openshift/tests/data
rulePath:
- ../jca.windup.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: an embedded resource adapter descriptor \(\'ra.xml\'\) *
            size: 1
      perform:
        fail:
        - message: JCA (ra.xml) usage was not found
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/openshift/tests/jca.windup.test.xml
