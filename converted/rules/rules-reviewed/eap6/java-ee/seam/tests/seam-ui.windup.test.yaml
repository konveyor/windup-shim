testDataPath: ../../windup/windup-rulesets/rules/rules-reviewed/eap6/java-ee/seam/tests/data
rulePath:
- ../seam-ui.windup.xml
ruleset:
- rules:
    rule:
    - when:
        not:
        - classification-exists:
          - classification: JSF Seam 2.x tag library usage
      perform:
        fail:
        - message: Classification for Seam 2.x Tag library not found!
    - when:
        not:
        - classification-exists:
          - classification: JSF Seam 2.3 tag library usage
      perform:
        fail:
        - message: Classification for Seam 2.3 Tag library not found!
    - when:
        not:
        - hint-exists:
          - message: .*Seam UI's `<s:button>`.*
      perform:
        fail:
        - message: Seam s:button not found.
    - when:
        not:
        - hint-exists:
          - message: .*should be replaced by `<h:link>`.*
      perform:
        fail:
        - message: Seam s:link not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:conversationId>`.*
      perform:
        fail:
        - message: Seam s:conversationId not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:conversationPropagation>`.*
      perform:
        fail:
        - message: Seam s:conversationPropagation not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:taskId>`.*
      perform:
        fail:
        - message: Seam s:taskId not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:defaultAction>`.*
      perform:
        fail:
        - message: Seam s:defaultAction not found.
    - when:
        not:
        - hint-exists:
          - message: .*should be replaced by `<f:convertDateTime.*
      perform:
        fail:
        - message: Seam s:convertDateTime not found.
    - when:
        not:
        - hint-exists:
          - message: .*Seam UI's `<s:convertEntity>` should be replaced by .*
      perform:
        fail:
        - message: Seam s:convertEntity not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:enumItem>`.*
      perform:
        fail:
        - message: Seam s:enumItem not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:token>`.*
      perform:
        fail:
        - message: Seam s:token not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:formattedText>`.*
      perform:
        fail:
        - message: Seam s:formattedText not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:cache>`.*
      perform:
        fail:
        - message: Seam s:cache not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:resource>`.*
      perform:
        fail:
        - message: Seam s:resource not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:download>`.*
      perform:
        fail:
        - message: Seam s:download not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:remote>`.*
      perform:
        fail:
        - message: Seam s:remote not found.
    - when:
        not:
        - hint-exists:
          - message: .*should be replaced by `<h:selectItems>`.*
      perform:
        fail:
        - message: Seam s:selectItems not found.
    - when:
        not:
        - hint-exists:
          - message: .*should be replaced by `<f:validateBean>`.*
      perform:
        fail:
        - message: Seam s:validateBean not found.
    - when:
        not:
        - hint-exists:
          - message: .*should be replaced by `<ui:fragment>`.*
      perform:
        fail:
        - message: Seam s:fragment not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:convertEnum>`.*
      perform:
        fail:
        - message: Seam s:convertEnum not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:convertAtomicBoolean>`.*
      perform:
        fail:
        - message: Seam s:convertAtomicBoolean not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:convertAtomicInteger>`.*
      perform:
        fail:
        - message: Seam s:convertAtomicInteger not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:convertAtomicLong>`.*
      perform:
        fail:
        - message: Seam s:convertAtomicLong not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:validateEquality>`.*
      perform:
        fail:
        - message: Seam s:validateEquality not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:validateAll>`.*
      perform:
        fail:
        - message: Seam s:validateAll not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:decorate>`.*
      perform:
        fail:
        - message: Seam s:decorate not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:div>`.*
      perform:
        fail:
        - message: Seam s:div not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:span>`.*
      perform:
        fail:
        - message: Seam s:span not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:label>`.*
      perform:
        fail:
        - message: Seam s:label not found.
    - when:
        not:
        - hint-exists:
          - message: .*Use `<h:message for=.*
      perform:
        fail:
        - message: Seam s:message not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:fileUpload>`.*
      perform:
        fail:
        - message: Seam s:fileUpload not found.
    - when:
        not:
        - hint-exists:
          - message: .*There is no direct mapping for `<s:graphicImage>`.*
      perform:
        fail:
        - message: Seam s:graphicImage not found.
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-reviewed/eap6/java-ee/seam/tests/seam-ui.windup.test.xml
