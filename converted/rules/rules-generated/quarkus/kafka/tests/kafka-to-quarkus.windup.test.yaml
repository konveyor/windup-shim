testDataPath: ../../windup/windup-rulesets/rules/rules-generated/quarkus/kafka/tests/data
rulePath:
- ../kafka-to-quarkus.windup.xml
- ../kafka-to-quarkus.windup.groovy
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Replace the `org.apache.kafka:kafka-clients` dependency with
                the Quarkus dependency.*
            size: 2
      perform:
        fail:
        - message: '[quarkus-kafka-client] quarkus-kafka-client Quarkus extension
            hint was not found!'
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Replace the `org.apache.kafka:kafka-streams` dependency with
                the Quarkus dependency.*
            size: 2
      perform:
        fail:
        - message: '[quarkus-kafka-streams] quarkus-kafka-streams Quarkus extension
            hint was not found!'
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-generated/quarkus/kafka/tests/kafka-to-quarkus.windup.test.xml
