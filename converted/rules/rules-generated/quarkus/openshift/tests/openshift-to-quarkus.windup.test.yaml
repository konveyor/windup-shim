testDataPath: ../../windup/windup-rulesets/rules/rules-generated/quarkus/openshift/tests/data
rulePath:
- ../openshift-to-quarkus.windup.xml
- ../openshift-to-quarkus.windup.groovy
ruleset:
- rules:
    rule:
    - when:
        not:
        - iterable-filter:
          - hint-exists:
            - message: Replace the `io.fabric8:openshift-client` dependency with the
                Quarkus dependency.*
            size: 2
      perform:
        fail:
        - message: '[quarkus-openshift-client] quarkus-openshift-client Quarkus extension
            hint was not found!'
  sourceFile: ""
sourceFile: http://github.com/windup/windup-ruletests/tree/master/rules/rules-generated/quarkus/openshift/tests/openshift-to-quarkus.windup.test.xml
